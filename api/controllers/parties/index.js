const { addParty } = require('./addParty');
const { deleteParty } = require('./deleteParty');
const { getAllParties } = require('./getAllParties');
const { updateParty } = require('./updateParty');
const { getParty } = require('./getParty');

module.exports = {
  addParty,
  deleteParty,
  getAllParties,
  updateParty,
  getParty
};
